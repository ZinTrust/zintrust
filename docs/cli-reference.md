# CLI Reference

## Core Commands

- `zin new <name>`: Create a new project
- `zin add <feature>`: Add a feature to existing project
- `zin migrate`: Run database migrations
- `zin debug`: Start debug dashboard
- `zin logs`: View application logs
