# Service Discovery

Service discovery guidance has been merged into [microservices.md](./microservices.md).

See that page for:

- the recommended `zin` microservices workflow
- filesystem discovery via `MicroserviceBootstrap`
- in-memory registration via `MicroserviceManager`
- health checks and allow-list behavior
