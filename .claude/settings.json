{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *zedgi/ThePlan.md) node zedgi/scripts/build-plan-html.mjs ;; esac; } || true",
            "statusMessage": "Rebuilding ThePlan.html"
          }
        ]
      }
    ]
  }
}
